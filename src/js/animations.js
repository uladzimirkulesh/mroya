import * as Animate from './animations/index';

/*-------------------------------------------------------
Fire on window load
-------------------------------------------------------*/

window.addEventListener( 'load', () => {
	// Init Animations
	Animate.sectionHero();
	Animate.sectionMission();
} );
